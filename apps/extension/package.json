{
  "name": "@input-assist/extension",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/copy-dictionary.mjs && vite build",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@input-assist/autocorrect-core": "workspace:*",
    "nspell": "^2.1.5"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.304",
    "dictionary-en": "^4.0.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.6",
    "vitest": "^2.1.8"
  }
}
